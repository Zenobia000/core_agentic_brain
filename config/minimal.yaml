# 極簡模式配置
# Layer 0 - 只啟用核心功能

version: "1.0"
mode: minimal

core:
  llm:
    provider: openai
    model: gpt-3.5-turbo
    temperature: 0.7
    max_tokens: 2000

  tools:
    enabled:
      - python
      - files