# --- 安全性 (絕對不能上傳) ---
.env
.env.local
*.key
*.pem

# --- Python 環境與暫存 ---
__pycache__/
*.py[cod]
.venv/
env/
venv/
.pytest_cache/

# --- RAG 專用資料 (避免專案過大) ---
# 向量資料庫本地儲存檔 (由 Docker 或程式自動產生)
qdrant_storage/
chroma_db/
faiss_index/

# 原始文件與處理後資料 (資料應由外部導入，或只上傳少量範例)
# 建議保留 data/sample 用於測試，其他忽略
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
data/indices/*

# --- IDE 設定 (師父可能用不同編輯器) ---
.vscode/
.idea/
*.swp